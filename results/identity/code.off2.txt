```python
def merge_sorted_lists(list1: list, list2: list) -> list:
    """
    Merge two sorted lists into a single sorted list.

    This function takes two lists that are already sorted in ascending order
    and combines them into one list that is also sorted in ascending order.
    It uses a two-pointer technique for an efficient O(n + m) time complexity,
    where n and m are the lengths of the two input lists.

    Args:
        list1: The first sorted list.
        list2: The second sorted list.

    Returns:
        A new list containing all elements from both input lists, sorted in ascending order.

    Examples:
        >>> merge_sorted_lists([1, 3, 5], [2, 4, 6])
        [1, 2, 3, 4, 5, 6]
        >>> merge_sorted_lists([], [1, 2, 3])
        [1, 2, 3]
        >>> merge_sorted_lists([1, 2, 3], [])
        [1, 2, 3]
        >>> merge_sorted_lists([], [])
        []
    """
    merged = []
    i, j = 0, 0

    # Compare elements from both lists and append the smaller one
    while i < len(list1) and